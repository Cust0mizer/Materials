Осуществляется через "," таким образом мы можем перечислять столбцы, которые нам нужны. Например:

SELECT last_name,
       percent_of_discount
FROM buyer - Выберет last_name и percent_of_discount из таблицы buyer